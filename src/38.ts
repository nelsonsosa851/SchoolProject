// This is a placeholder file for your reference. Replace it with actual TypeScript code.
// Example:
function greet(name: string): void {
  console.log(`Hello, ${name}!`);
}

// Call the function to test
greet("Alice");
